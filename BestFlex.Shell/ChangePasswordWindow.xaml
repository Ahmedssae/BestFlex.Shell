<Window x:Class="BestFlex.Shell.ChangePasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:BestFlex.Shell.Converters"
        Title="Change Password" Height="350" Width="420"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Window.Resources>
        <local:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>
        <local:BooleanToVisibilityInvertedConverter x:Key="BooleanToVisibilityInvertedConverter"/>
    </Window.Resources>
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Current Password" FontWeight="SemiBold" Margin="0,0,0,4"/>
        <PasswordBox x:Name="CurrentBox" Grid.Row="2" Height="32" FontSize="14"/>

        <TextBlock Grid.Row="4" Text="New Password" FontWeight="SemiBold" Margin="0,0,0,4"/>
        <PasswordBox x:Name="NewBox" Grid.Row="5" Height="32" FontSize="14"/>

        <TextBlock Grid.Row="6" Text="Confirm New Password" FontWeight="SemiBold" Margin="0,0,0,4"/>
        <PasswordBox x:Name="ConfirmBox" Grid.Row="7" Height="32" FontSize="14"/>

        <TextBlock x:Name="ErrorText" Grid.Row="8" Text="{Binding ValidationMessage}" Foreground="Red" 
                   VerticalAlignment="Center" Margin="0,0,12,0" 
                   Visibility="{Binding ValidationMessage, Converter={StaticResource StringToVisibilityConverter}}"/>
        
        <TextBlock x:Name="MatchText" Grid.Row="9" Text="Passwords do not match" Foreground="Orange" 
                   VerticalAlignment="Center" Margin="0,0,12,0" 
                   Visibility="{Binding ConfirmPasswordMatch, Converter={StaticResource BooleanToVisibilityInvertedConverter}}"/>

        <StackPanel Grid.Row="11" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button x:Name="SaveButton" Content="Change Password" Width="120" Height="35" 
                    Command="{Binding ChangePasswordCommand}" IsDefault="True" Margin="0,0,8,0"/>
            <Button x:Name="CancelButton" Content="Cancel" Width="100" Height="35" 
                    Click="Cancel_Click" Margin="0,0,8,0"/>
        </StackPanel>
    </Grid>
</Window>
