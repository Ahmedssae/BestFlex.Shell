<Window x:Class="BestFlex.Shell.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="BestFlex — Main Menu" Height="500" Width="980"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Theme/Theme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
  

    <DockPanel>

        <!-- === Top Menu === -->
        <Menu DockPanel.Dock="Top" Background="#2f343d" Foreground="White">
            <MenuItem Header="_Account">
                <MenuItem Header="Change _Password" Click="ChangePassword_Click"/>
                <Separator/>
                <MenuItem Header="_Sign Out" Click="SignOut_Click"/>
                <MenuItem Header="_Settings" Width="120" Click="OpenSettings_Click"/>
            </MenuItem>
        </Menu>

        <!-- === Layout: Sidebar + Main Host === -->
        <Grid Margin="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="230"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Sidebar -->
            <Border Grid.Column="0"
                    Background="#F9FAFB"
                    BorderBrush="#E5E7EB"
                    BorderThickness="1"
                    CornerRadius="8">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel x:Name="Sidebar" Margin="8"/>
                </ScrollViewer>
            </Border>

            <!-- Spacer -->
            <Rectangle Grid.Column="1" Fill="Transparent"/>

            <!-- Main content HOST (navigation area) -->
            <Border Grid.Column="2"
                    Background="#FFFFFF"
                    BorderBrush="#E5E7EB"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="12">
                <ContentControl x:Name="MainHost"/>
            </Border>
        </Grid>
    </DockPanel>
</Window>
