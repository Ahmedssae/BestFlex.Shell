<Window x:Class="BestFlex.Shell.Views.CustomerListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Customer Management" Height="600" Width="900"
        WindowStartupLocation="CenterScreen">
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Customer Management" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <!-- Search and Filters -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox Text="{Binding SearchTerm, UpdateSourceTrigger=PropertyChanged}" 
                     Width="200" Height="30" Margin="0,0,10,0"
                     VerticalContentAlignment="Center"/>
            <CheckBox Content="Include Inactive" IsChecked="{Binding IncludeInactive}" 
                      VerticalAlignment="Center" Margin="0,0,10,0"/>
            <Button Content="Refresh" Command="{Binding RefreshCommand}" 
                    Width="80" Height="30" Margin="0,0,10,0"/>
            <Button Content="Add Customer" Command="{Binding AddCustomerCommand}" 
                    Width="100" Height="30" Margin="0,0,10,0"/>
            <Button Content="Edit" Command="{Binding EditCustomerCommand}" 
                    Width="80" Height="30" Margin="0,0,10,0"/>
            <Button Content="Deactivate" Command="{Binding DeactivateCustomerCommand}" 
                    Width="100" Height="30"/>
        </StackPanel>

        <!-- Customer List -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding Customers}" 
                  SelectedItem="{Binding SelectedCustomer}"
                  AutoGenerateColumns="False" 
                  CanUserAddRows="False" 
                  CanUserDeleteRows="False"
                  IsReadOnly="True"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="200"/>
                <DataGridTextColumn Header="Tax ID" Binding="{Binding TaxId}" Width="120"/>
                <DataGridTextColumn Header="Credit Limit" Binding="{Binding CreditLimit, StringFormat=C}" Width="120"/>
                <DataGridTextColumn Header="Payment Terms" Binding="{Binding PaymentTermsDays}" Width="100"/>
                <DataGridTextColumn Header="Status" Binding="{Binding IsActive, Converter={StaticResource BoolToActiveConverter}}" Width="80"/>
                <DataGridTextColumn Header="Created" Binding="{Binding CreatedAt, StringFormat=yyyy-MM-dd}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Status Bar -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,10,0,0">
            <ProgressBar IsIndeterminate="True" Height="20" Width="200" 
                         Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"/>
            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" 
                       Margin="10,0,0,0" VerticalAlignment="Center"/>
        </StackPanel>
    </Grid>
</Window>
