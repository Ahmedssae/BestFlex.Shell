<Window x:Class="BestFlex.Shell.Views.ProductEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="{Binding Title}" Height="500" Width="600"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="{Binding Title}" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>

        <!-- Form -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <Grid Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Label Grid.Row="0" Grid.Column="0" Content="SKU:" VerticalAlignment="Center"/>
                    <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding SKU, UpdateSourceTrigger=PropertyChanged}" 
                             Height="30" VerticalContentAlignment="Center" IsEnabled="{Binding ProductId, Converter={StaticResource NullToBooleanConverter}}"/>

                    <Label Grid.Row="1" Grid.Column="0" Content="Name:" VerticalAlignment="Center" Margin="0,10,0,0"/>
                    <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" 
                             Height="30" VerticalContentAlignment="Center" Margin="0,10,0,0"/>

                    <Label Grid.Row="2" Grid.Column="0" Content="Description:" VerticalAlignment="Center" Margin="0,10,0,0"/>
                    <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" 
                             Height="60" VerticalContentAlignment="Top" TextWrapping="Wrap" Margin="0,10,0,0"
                             AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>

                    <Label Grid.Row="3" Grid.Column="0" Content="Cost:" VerticalAlignment="Center" Margin="0,10,0,0"/>
                    <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding Cost, UpdateSourceTrigger=PropertyChanged}" 
                             Height="30" VerticalContentAlignment="Center" Margin="0,10,0,0"/>

                    <Label Grid.Row="4" Grid.Column="0" Content="Base Price:" VerticalAlignment="Center" Margin="0,10,0,0"/>
                    <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding BasePrice, UpdateSourceTrigger=PropertyChanged}" 
                             Height="30" VerticalContentAlignment="Center" Margin="0,10,0,0"/>
                </Grid>

                <!-- Price Tiers Section -->
                <GroupBox Header="Price Tiers" Margin="0,20,0,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Button Grid.Row="0" Content="Add Price Tier" Command="{Binding AddPriceTierCommand}" 
                                Width="120" Height="30" HorizontalAlignment="Left" Margin="0,10,0,10"/>

                        <DataGrid Grid.Row="1" ItemsSource="{Binding PriceTiers}" 
                                  AutoGenerateColumns="False" 
                                  CanUserAddRows="False" 
                                  CanUserDeleteRows="True"
                                  Height="120">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Qty From" Binding="{Binding QuantityFrom}" Width="80"/>
                                <DataGridTextColumn Header="Qty To" Binding="{Binding QuantityTo}" Width="80"/>
                                <DataGridTextColumn Header="Price" Binding="{Binding Price, StringFormat=C}" Width="100"/>
                                <DataGridTextColumn Header="Currency" Binding="{Binding Currency}" Width="80"/>
                                <DataGridTemplateColumn Header="Actions" Width="*">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Content="Remove" 
                                                    Command="{Binding DataContext.RemovePriceTierCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                    CommandParameter="{Binding}"
                                                    Width="60" Height="25"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </GroupBox>

                <!-- Validation Errors -->
                <ItemsControl ItemsSource="{Binding ValidationErrors}" Margin="0,10,0,0">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" FontSize="12" Margin="0,2,0,0"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>

        <!-- Status Message -->
        <TextBlock Grid.Row="2" Text="{Binding ErrorMessage}" Foreground="Red" 
                   Margin="0,10,0,0" VerticalAlignment="Center" TextWrapping="Wrap"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Save" Command="{Binding SaveCommand}" Width="80" Height="30" Margin="0,0,10,0"/>
            <Button Content="Cancel" Command="{Binding CancelCommand}" Width="80" Height="30"/>
        </StackPanel>

        <!-- Loading Overlay -->
        <Grid Grid.RowSpan="4" Background="#80000000" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Height="20" Width="200" Margin="0,0,0,10"/>
                <TextBlock Text="Processing..." Foreground="White" HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
